apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway-proxy
  namespace: kube-gloo-system
spec:
  template:
    spec:
      containers:
        - name: gateway-proxy
          resources:
            requests:
              cpu: 100m
            limits:
              cpu: 500m

