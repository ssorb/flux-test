apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: workloads
  namespace: flux-system
spec:
  interval: 1m
  path: ./workloads
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system

